<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Página Principal</title>
  <link rel="stylesheet" href="/styles.css" />
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(to bottom, #2c587f, #c8e0f7);
      display: flex;
      justify-content: center;
      height: 100vh;
    }

    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
      width: 100%;
      max-width: 1200px;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      width: 100%;
      background-color: #c8e0f7;
      padding: 10px 20px;
      border-radius: 12px;
      color: black;
      margin-top: 20px;
    }

    .header .user-info span {
      color: black;
      font-weight: bold;
      margin-left: 10px;
    }

    .header .menu-icon {
      font-size: 20px;
      cursor: pointer;
    }

    .main-content {
      display: flex;
      gap: 20px;
      width: 100%;
      margin-top: 20px;
    }

    .balance-card {
      background: #c8e0f7;
      color: black;
      padding: 15px;
      border-radius: 10px;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
      flex: 1;
      height: 200px; /* Altura aumentada */
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    
      .boton-pildora {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 10px 15px;
    border-radius: 25px;
    font-weight: bold;
    text-decoration: none;
    font-size: 14px;
    border: 2px solid #003087;
    background-color: #e6f0ff;
    color: #003087;
    transition: background-color 0.3s ease, color 0.3s ease;
  }

  .boton-pildora:hover {
    background-color: #003087;
    color: white;
  }

  .boton-pildora.activo {
    background-color: #003087;
    color: white;
  }


    .activity-card {
      background: #c8e0f7;
      color: black;
      padding: 15px;
      border-radius: 10px;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
      flex: 1;
      height: 500px;
      display: flex;
      flex-direction: column;
      justify-content: space-between; /* Asegura que el contenido se distribuya */
      position: relative; /* Para usar position absoluta en el botón */
    }

    .actions-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 15px;
      margin-top: 20px;
    }

    .action-button {
      background: #ffffff;
      color: #000;
      text-align: center;
      padding: 10px;
      border-radius: 8px;
      box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
      cursor: pointer;
      font-weight: bold;
      font-size: 14px;
    }

    .footer {
      display: flex;
      justify-content: space-evenly;
      align-items: center;
      width: 100%;
      background-color: #002c6c;
      padding: 10px 0;
      margin-top: 20px;
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      color: white;
      box-shadow: 0px -2px 4px rgba(0, 0, 0, 0.1);
      z-index: 1000;
    }

    .footer-icon img {
      width: 20px;
      height: 20px;
      object-fit: cover;
    }

    /* Ocultar actividades extras */
    .hidden-activity {
      display: none;
    }

    /* Botón ver más fijo abajo */
    #toggle-activities-btn {
    background: #004080;
    color: white;
    border: none;
    padding: 10px;
    font-weight: bold;
    cursor: pointer;
    border-radius: 10px; /* Botón con bordes redondeados */
    box-shadow: 0px -2px 5px rgba(0, 0, 0, 0.2);
    position: absolute;
    bottom: 15px; /* Alineado con el padding del contenedor */
    left: 15px;
    right: 15px; /* Coincide con el padding */
  }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <div class="user-info">
        <img src="/logoVaultAzul.png" alt="Logo" width="20" height="20" />
        <span>¡Hola, <%= @usuario.person.name %>!</span>
      </div>
      <div class="menu-icon">☰</div>
    </div>

    <!-- Main Content -->
    <div class="main-content">
      <!-- Balance Section -->
      <div class="balance-card">
        <div>
          <div style="display: flex; align-items: center; gap: 10px;">
            <h2 id="saldo">$<%= formatted_money(@cuenta&.balance || 0) %></h2>
            <button onclick="toggleSaldo()" style="background: none; border: none; cursor: pointer;">
              <i id="icono-ojo" class="fa-solid fa-eye"></i>
            </button>
          </div>
          <button onclick="mostrarCVU()">Mi CVU</button>
          <!-- Recuadro oculto con CVU y alias -->
          <div
            id="cvu-info"
            style="display: none; margin-top: 10px; background: white; padding: 10px; border-radius: 8px; color: black; box-shadow: 0 2px 6px rgba(0,0,0,0.2);"
          >
            <p><strong>CVU:</strong> <%= @cuenta&.cvu || 'No disponible' %></p>
            <p><strong>Alias:</strong> <%= @cuenta&.account_alias || 'No disponible' %></p>
          </div>
        </div>
        <div class="actions-grid">
          <a href="/ingresar" class="boton-pildora">Ingresar</a>
          <a href="/transferir" class="boton-pildora">Transferir</a>
          <a href="/retirar" class="boton-pildora">Retirar</a>
          <a href="/ahorros" class="boton-pildora">Ahorros</a>
          <a href="/reserva">
            <img src="/ahorros.png" alt="reserva" style="width: 50px; height: 50px; cursor: pointer;" />
          </a>

        </div>
      </div>

      <!-- Activity Section -->
      <div class="activity-card">
        <h3>Actividad</h3>
        <% if @activities.any? %>
          <ul style="list-style-type: none; padding: 0;">
            <% @activities.each_with_index do |activity, index| %>
              <li
                class="<%= 'hidden-activity' if index >= 4 %>"
                style="margin-bottom: 10px; padding: 10px; border-bottom: 1px solid #ddd;"
              >
                <span><strong><%= activity[:type] %>:</strong></span>
                <%
                  color = if activity[:type] == 'Reserva'
                              '#f4d03f'  
                            elsif activity[:amount] < 0
                              'red'
                            else
                              'green'
                            end
                  %>
                  <span style="color: <%= color %>;">
                    <%= sprintf("$%.2f", activity[:amount]) %>
                  </span>

                <p style="margin: 5px 0;"><%= activity[:details] %></p>
                <small style="color: #666;"><%= activity[:date].strftime("%d/%m/%Y %H:%M") %></small>
              </li>
            <% end %>
          </ul>
          <% if @activities.size > 4 %>
            <a href="/actividades" id="toggle-activities-btn" class="action-button">Ver más</a>
          <% end %>
        <% else %>
          <p>No hay actividad reciente.</p>
        <% end %>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <div class="footer-icon">
        <img src="/icon1.png" alt="Ícono 1" />
      </div>
      <div class="footer-icon">
        <img src="/icon2.png" alt="Ícono 2" />
      </div>
      <div class="footer-icon">
        <img src="/icon3.png" alt="Ícono 3" />
      </div>
    </div>
  </div>

  <script>
    function mostrarCVU() {
      const cvuInfo = document.getElementById("cvu-info");
      if (cvuInfo.style.display === "none") {
        cvuInfo.style.display = "block";
      } else {
        cvuInfo.style.display = "none";
      }
    }

    function toggleSaldo() {
    const saldo = document.getElementById("saldo");
    const icono = document.getElementById("icono-ojo");
    const valorReal = saldo.getAttribute("data-real");

    if (!valorReal) {
      saldo.setAttribute("data-real", saldo.textContent);
      saldo.textContent = "$***";
      icono.classList.remove("fa-eye");
      icono.classList.add("fa-eye-slash");
    } else {
      saldo.textContent = valorReal;
      saldo.removeAttribute("data-real");
      icono.classList.remove("fa-eye-slash");
      icono.classList.add("fa-eye");
    }
  }
  </script>
</body>
    <script>
</script>
</html>